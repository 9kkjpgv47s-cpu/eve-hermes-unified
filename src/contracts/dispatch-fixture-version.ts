/**
 * Version for checked-in `fixtures/dispatch/*.json` files.
 * Bump when fixture shape or semantic expectations change so CI can pin migrations.
 */
export const DISPATCH_FIXTURE_SCHEMA_VERSION = 1;
